import TextInput from "./TextInput";

function Styling() {
    const name = "Richard";
    return (
        <div>
            <TextInput name={name} />
        </div>
    );
}

export default Styling;
